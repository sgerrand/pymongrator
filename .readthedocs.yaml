version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  jobs:
    install:
      - pip install uv
      - uv sync --group docs
    build:
      html:
        - uv run zensical build
        - mkdir -p $READTHEDOCS_OUTPUT/html
        - cp -r site/. $READTHEDOCS_OUTPUT/html
